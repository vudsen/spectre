spring:
  liquibase:
    change-log: classpath:/db-test/changelog-master.xml
  datasource:
    url: jdbc:sqlite:identifier-test.sqlite
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.community.dialect.SQLiteDialect
logging:
  level:
    io.github.vudsen.spectre.core.service.impl.DefaultArthasExecutionService: debug