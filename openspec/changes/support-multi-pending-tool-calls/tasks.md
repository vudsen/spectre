## 1. 后端批次挂起模型

- [x] 1.1 将 `DefaultAiService` 的单工具循环改为按 assistant message 收集整批 `toolCalls`，在存在阻塞项时统一生成待处理批次。
- [x] 1.2 扩展 `AgentEventPublisher` 和 SSE 消息结构，为批次内每个工具补充 `toolCallId`、顺序和待处理状态字段。
- [x] 1.3 重写 `ChatMemoryUtil` 的未响应工具定位逻辑，使其能够返回最后一条 assistant message 的完整未完成工具列表。

## 2. 前端批次展示与交互

- [x] 2.1 调整 AI 消息类型、卡片拼装逻辑和工具段索引方式，使前端按 `toolCallId` 展示同一轮内的多个工具调用。
- [x] 2.2 实现多个确认框并发显示、多个 `ask_human` 串行显示，以及“等待执行”工具的只读状态展示。
- [x] 2.3 在会话恢复前汇总整批工具响应，按原始顺序组装结构化请求体并调用恢复接口。

## 3. 恢复执行与验证

- [x] 3.1 在后端实现整批恢复校验，确保前端提交的数量、顺序和 `toolCallId` 与最后一条未完成 assistant message 完全一致。
- [x] 3.2 按原始顺序执行批准的确认工具、等待执行工具，并为拒绝项和 `ask_human` 项生成对应的工具响应。
- [x] 3.3 为多确认、多 `ask_human`、混合阻塞项和同名工具调用补充后端与前端测试用例。
