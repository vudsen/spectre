interface BatchChannelCreateProps {
  channels: { treeNodeId: string; bundleId: string; runtimeNodeId: string }[]
}

const BatchChannelCreate: React.FC<BatchChannelCreateProps> = (props) => {
  // TODO
}

export default BatchChannelCreate
